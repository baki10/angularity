dependencies {
  compile project(':market:server')
  compile group: 'org.springframework', name: 'spring-webmvc', version: rootProject.ext.springVersion
  compile 'javax.servlet:servlet-api:'+rootProject.ext.servletVersion
}